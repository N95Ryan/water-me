---
import Layout from '../layouts/Layout.astro';
import { Image } from 'astro:assets';
import logo from '../assets/logo.png';
---

<Layout title="Water Me - Stay Hydrated">
  <div class="flex flex-col items-center justify-center min-h-screen bg-gradient-to-br from-[#00617a] via-[#206f86] to-[#618c9f] text-white">
    <!-- Header -->
    <header class="w-full max-w-4xl py-4 sm:py-6 px-4 flex justify-center items-center">
      <Image 
        src={logo} 
        alt="Water Me Logo" 
        class="h-20 sm:h-32 w-auto drop-shadow-lg hover:drop-shadow-xl transition-all duration-300" 
        width={300}
        height={300}
        quality="high"
      />
    </header>

    <!-- Contenu principal -->
    <main class="flex flex-col items-center text-center p-4 sm:p-8">
      <h2 class="text-2xl sm:text-4xl font-bold mb-3 sm:mb-4">Restez hydraté, sans effort !</h2>
      <p class="text-base sm:text-lg text-gray-200 max-w-lg px-4">
        Programmez des rappels d'hydratation et laissez Water Me vous aider à boire la quantité d'eau idéale chaque jour.
      </p>
      <div class="flex flex-col sm:flex-row gap-4 mt-4 sm:mt-6">
        <a href="/reminders" class="px-6 py-3 bg-[#ffffff] text-[#206f86] rounded-lg text-base sm:text-lg font-semibold shadow-lg hover:bg-gray-200 transition-colors">
          Démarrer
        </a>
        <button class="px-6 py-3 bg-white text-[#206f86] rounded-lg text-base sm:text-lg font-semibold shadow-lg hover:bg-gray-200 transition-colors">
          Se connecter
        </button>
      </div>
    </main>

    <!-- Pied de page -->
    <footer class="absolute bottom-4 text-gray-300 text-xs sm:text-sm px-4 text-center">
      © 2025 Water Me. Stay Hydrated. 💙
    </footer>
  </div>
</Layout>
